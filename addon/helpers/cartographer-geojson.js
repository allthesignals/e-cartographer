import { helper } from '@ember/component/helper';

// placeholder for munging into generic interface
export function cartographerGeojson(params/*, hash*/) {
  return params;
}

export default helper(cartographerGeojson);
